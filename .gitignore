# dependencies (bun install)
node_modules

# output
out
dist
target
target-vst
target-launcher
plugins/**/target
*.tgz

# Compiled aurora CLI + embedded Docker context / native vendor cache
/aurora
/aurora.exe
cli/.embedded/
cli/.vendor/
dist-cli/
.aurora/

# code coverage
coverage
*.lcov

# logs
logs
*.log
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json
.playwright-mcp/

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local
.env.github-app

# caches
.eslintcache
.cache
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store
/.cursor/
/CLAUDE.md

# Generated shader outputs (from `bun run build:shaders` / TypeGPU + variants)
# Source of truth is under shaders/ (TypeGPU *.ts + shared helpers; variants/*.wgsl kept for re-bootstrap)
assets/shaders/vj_grid.wgsl
assets/shaders/vj_palette.wgsl

# Scratch directory used during shader bundling (cleaned by the script)
temp_scratch/
*.jpeg
*.png
# Large / local-only glTF packs (show machines only — not GH Pages).
# Web pack (ship:true): cesium-man, fox, duck, rigged-figure, damaged-helmet,
# cesium-milk-truck — those stay tracked. See models/SOURCES.md.
models/human-female/
models/antique-camera/
models/avocado/
models/boom-box/
models/brain-stem/
models/corset/
models/iridescent-dish/
models/lantern/
models/sheen-chair/
models/toy-car/
models/water-bottle/
models/**/*.zip
models/3daistock.zip
